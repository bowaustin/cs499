included makefile will correctly compile the code using the command:
g++ -O3 -o filedb main.cpp

note: the program assumes that the files will be named personnel_salaries.txt and personnel_addresses.txt respectivly and will inform the user that a file could not be opened if either of those files cannot be found.
